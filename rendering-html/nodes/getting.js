
export const getHtmlChildNode = (node, index) => node.childNodes[index]

export const getHtmlChildNodes = (node) => Array.from(node.childNodes)

export const getHtmlParentNode = (node) => node.parentNode